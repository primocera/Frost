# Host of the deployed PartyKit game server (NO protocol, NO trailing slash).
# Local dev leaves this unset and the client uses localhost:1999.
# In production (e.g. Vercel) set this to your deployed PartyKit URL:
#   VITE_PARTYKIT_HOST=frost.<your-account>.partykit.dev
VITE_PARTYKIT_HOST=
