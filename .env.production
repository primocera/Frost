# Deployed Cloudflare Worker host (public URL — safe to commit). Baked into the
# production build so the site connects to multiplayer automatically.
VITE_PARTYKIT_HOST=frost.primoz2-cerar.workers.dev
